import React from "react";
import './index.scss';
import Layout from '../components/layout';

export default () => (
    <div>
        <Layout>
            <h2>Home</h2>
        </Layout>
    </div>
);
